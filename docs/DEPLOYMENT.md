# Deployment

- `php artisan config:cache`
- `php artisan route:cache`
- `php artisan view:cache`
- Worker queue (database): `php artisan queue:work`
- Cron: `php artisan schedule:run` (chaque minute)
